/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> } 
 */
exports.seed = async function(knex) {
  // Deletes ALL existing entries



 
  const defaultProjects=[{project_name:"React ile Web Sitesi Tasarlama",project_description:"React kütüphanesi ile form ekranı oluşturma"}]




  
};
